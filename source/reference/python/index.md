# Python API Reference

Auto-generated reference for the `pygenogrove` extension module, built directly
from the binding docstrings and signatures. For tutorials, worked examples, and
the conceptual model, see the Python tabs throughout the {doc}`User Guide
</user_guide>`.

:::{note}
These pages are generated by `autodoc` and therefore require `pygenogrove` to be
installed in the documentation build environment (`pip install pygenogrove`). A
C++-only docs build omits this section.
:::

```{toctree}
:maxdepth: 2

coordinates
grove
typed_groves
io
registry
```